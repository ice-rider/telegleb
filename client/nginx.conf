server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied any;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/x-javascript application/xml image/svg+xml;
    gzip_disable "MSIE [1-6]\.";

    brotli on;
    brotli_types text/plain text/css text/xml text/javascript application/javascript application/x-javascript application/xml image/svg+xml;
    brotli_comp_level 6;

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|webp|avif|woff2|ttf|eot)$ {
        expires 1y;
        access_log off;
        add_header Cache-Control "public, immutable";
        # Свой add_header здесь отменяет серверные, поэтому нужные заголовки
        # повторяются явно.
        add_header X-Content-Type-Options "nosniff" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    }

    # add_header в location отменяет ВСЕ унаследованные из server, поэтому
    # заголовки задаются одним блоком на уровне server и не дублируются в
    # location — иначе часть из них молча теряется.
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; media-src 'self' blob:; font-src 'self'; connect-src 'self'; frame-src 'none'; object-src 'none';" always;
    add_header Permissions-Policy "geolocation=(), midi=(), camera=(), usb=(), magnetometer=(), accelerometer=(), gyroscope=(), fullscreen=(self), payment=()" always;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
